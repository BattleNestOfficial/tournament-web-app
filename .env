# Database connection string (e.g., postgresql://user:password@host:port/database)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/mydb"

# Secret key for signing JWTs. Should be a long, random string.
# You can generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET="your-super-secret-session-key-that-is-long"

# Your Google Client ID for enabling Google Sign-In.
# Get this from the Google Cloud Console under APIs & Services > Credentials.
GOOGLE_CLIENT_ID="51856792385-f56mh6nch7c6thkimc44f2if8bp5srmp.apps.googleusercontent.com"

# (Optional) OCR API Key for result extraction
# OCR_SPACE_API_KEY="your_ocr_space_api_key"

# (Optional) Email service (Brevo/Sendinblue) API key
# BREVO_API_KEY="your_brevo_api_key"
